What is the routing?
    Routing in Angular refers to the process of navigating between different views or pages within an application.
    Angular provides a powerful routing mechanism that allows you to define routes, associate them with components, and handle navigation between these routes.

setup Router:
    1. import RouterModule
    2. configure forRoot once this method allow us to add multiple route config.
    
Feature Module and Rounting:
    
In Angular, a feature module is a module that encapsulates a specific feature or functionality of your application.
It helps organize and modularize your application by grouping related components, services, and other resources together.

LazyLoading:
    
route Gaurds:
    this gaurds are useful to secure the routes.
    5 types:
        1.canActivate
        2.canActivateChild
        3.canDeactivate
        4.canLoad
        5.Resolve
    Routing Event:
        